#include <stdio.h>
/**
 * main - a program that prints all single digit number
 * Return: Always (Success)
 */
int main(void)
{
int i = 0;
while (i < 10)
{
printf("%d ", i);
i++;
}
printf("\n");
return (0);
}
